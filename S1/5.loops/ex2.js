//COMPRENDIDO


for (let index = 0; index < 10; index++) { // bucle de 0 a 9
    if(index % 2 === 0){                   // si bucle / 2 = 0
        console.log(index)                 // imprime el index (imprime los pares)
    }
}